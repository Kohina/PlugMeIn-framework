= Things to fix and work with =

< in progress > = someone is working on this task/class

< done > = the task is done

- *RAD* Finish sections 2 (muck-ups) 

- *Use cases* define use cases for new possible plugins

- *Connections* Create some way for a client to assign a name to itself and send this to the server so that it always knows where to send a particular PluginCall and so that we can notify clients of other clients.

- *SDD* Complete

- *Closing* Create some way to do a controlled closing of the application, closing streams and deleting temporary files.